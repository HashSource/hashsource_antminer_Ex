int sub_245D1C()
{
  return sub_539E8(
           "tsave",
           7,
           (int)sub_2459F8,
           (int)"Save the trace data to a file.\n"
                "Use the '-ctf' option to save the data to CTF format.\n"
                "Use the '-r' option to direct the target to save directly to the file,\n"
                "using its own filesystem.");
}
