int sub_16504()
{
  int v0; // r0

  v0 = sub_94728(
         "target-memory.c",
         144,
         "%s: Assertion `%s' failed.",
         "void block_boundaries(CORE_ADDR, CORE_ADDR*, CORE_ADDR*)",
         "region->attrib.mode == MEM_FLASH");
  return sub_16538(v0);
}
